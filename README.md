#### WIP: This is a the repository for NextGen Packaging app.

#### WIP: Local devloop:

1. Install es-lint + prettier package from extensions.
2. ensure you have `format on save` set to `on`
3. ensure you have `Default Formatter` set to `Prettier`

-> There is a pre-commit hook that should fix any eslint and prettier if
not caught with `cmd + s`.
